# Docker configuration
# These values work for containerized deployment
IP_ADDRESS = "0.0.0.0"  # Bind to all interfaces in container
PORT = 8443
SSL_KEYFILE = "key.pem"
SSL_CERTFILE = "cert.pem"
